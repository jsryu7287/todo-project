# PRD: Todo 일정관리 앱

## 개요

개인 할 일을 브라우저에서 관리하는 단일 파일 웹 애플리케이션.  
별도 설치 없이 `index.html`을 열어 바로 사용할 수 있으며, 데이터는 Supabase 클라우드 DB에 저장되어 기기 간 공유가 가능하다.

---

## 목표

- 단순하고 빠르게 할 일을 추가·완료·삭제할 수 있는 UI 제공
- 서버 없이 정적 HTML 파일 하나로 실행 가능
- 새로고침·기기 전환 후에도 데이터 유지 (클라우드 DB 연동)

---

## 사용자 스토리

| # | 스토리 | 완료 기준 |
|---|---|---|
| 1 | 사용자는 텍스트를 입력해 할 일을 추가할 수 있다 | 입력 후 버튼 클릭 또는 Enter → 목록에 즉시 표시 |
| 2 | 사용자는 할 일을 완료 처리할 수 있다 | 체크박스 클릭 → 취소선 + 투명도 변경 |
| 3 | 사용자는 완료를 취소할 수 있다 | 체크 해제 → 미완료 상태 복귀 |
| 4 | 사용자는 할 일을 삭제할 수 있다 | ✕ 버튼 클릭 → 목록에서 제거 |
| 5 | 사용자는 전체/미완료/완료 항목만 볼 수 있다 | 필터 탭 클릭 → 해당 항목만 표시 |
| 6 | 사용자는 완료/전체 개수를 확인할 수 있다 | 목록 상단에 "완료 N / 전체 N" 표시 |
| 7 | 공백만 입력하면 추가되지 않는다 | 빈 입력 무시, 포커스 유지 |
| 8 | 다른 기기·브라우저에서도 같은 데이터를 볼 수 있다 | Supabase DB 저장, 페이지 로드 시 조회 |

---

## 기능 명세

### 핵심 기능

#### 할 일 추가
- 입력창에 텍스트 입력 후 `추가` 버튼 클릭 또는 `Enter` 키
- 최대 200자
- 추가 후 입력창 초기화, 포커스 유지
- 추가 중 입력창·버튼 비활성화 (중복 제출 방지)

#### 완료 체크
- 체크박스 클릭으로 토글
- 완료 상태: 취소선 + 투명도 0.5

#### 삭제
- ✕ 버튼 클릭으로 즉시 삭제
- 삭제 중 해당 항목 반투명 처리

#### 필터
- **전체**: 모든 항목 표시
- **미완료**: `done = false` 항목만
- **완료**: `done = true` 항목만
- 활성 필터 탭은 보라색 강조

### UX

| 상태 | 처리 |
|---|---|
| 데이터 로딩 중 | 스켈레톤 카드 3개 표시 |
| 목록이 비었을 때 | "할 일이 없습니다 🎉" 메시지 |
| DB 오류 | "데이터를 불러오지 못했습니다." 에러 메시지 |
| 항목 조작 중 | 해당 항목 반투명 + 이벤트 차단 |

---

## 기술 스택

| 구분 | 기술 |
|---|---|
| 프론트엔드 | HTML5 / CSS3 / Vanilla JS (ES2020+) |
| 데이터베이스 | Supabase (PostgreSQL) |
| SDK | `@supabase/supabase-js@2` (CDN) |
| 테스트 | Playwright (E2E, `todo.test.js`) |
| 저장소 | GitHub (`jsryu7287/todo-project`) |

---

## 데이터 모델

### `todo_items` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | uuid | PK, 자동 생성 |
| `text` | text | 할 일 내용 |
| `done` | boolean | 완료 여부 (기본값: false) |
| `created_at` | timestamptz | 생성 시각 (기본값: now()) |

- RLS 활성화, 익명 사용자 전체 접근 허용 (인증 없는 공개 앱)
- 조회 정렬: `created_at DESC`

---

## 비기능 요구사항

- **접근성**: 체크박스·삭제 버튼에 `aria-label` 부여
- **반응형**: 최대 너비 520px, 모바일 대응
- **의존성**: 런타임 외부 의존성은 Supabase JS CDN 하나뿐
- **호환성**: 모던 브라우저 (ES2020+ 지원 환경)

---

## 범위 외 (Out of Scope)

- 사용자 인증 / 계정별 데이터 분리
- 마감일 / 우선순위 / 카테고리
- 드래그 앤 드롭 정렬
- 오프라인 지원 (PWA)
- 서버사이드 렌더링
